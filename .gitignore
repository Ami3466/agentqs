node_modules
# every env flavour (.env.production, .env.development.local, …) — but keep a committed example
.env*
!.env.example
# /data* also catches iCloud resurrections/renames ("data 2", data.stray-*)
/data*
# A root-level record store (if someone runs agentqs with its data dir at the checkout root).
# Anchored so samples/record/ stays tracked.
/record/
/tmp/
*.db
.DS_Store
dist/
.next/
.next-e2e/
.next-*/
tsconfig.tsbuildinfo
tsconfig*.tsbuildinfo
CLAUDE.local.md

# Agent guide — kept local, not shipped in the public repo
CLAUDE.md
